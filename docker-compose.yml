services:
  pipeline:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/pipeline.db
    volumes:
      - pipeline-data:/data
    restart: unless-stopped

volumes:
  pipeline-data:
